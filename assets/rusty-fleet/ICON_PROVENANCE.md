# Rusty Fleet site icon provenance

The onboarding page references:

```text
/assets/rusty-fleet/fleet-icon.svg
```

This is a byte-for-byte copy of the generic canonical source
`assets/branding/rusty-fleet.svg` from the corresponding local Rusty Fleet
source checkpoint. Its SHA-256 is
`1dedfecaef954dda9bb6f4f133376535e4799908441e7832558a1f70f4ed6f79`.

The vector uses simple repository-native geometry and contains no third-party
artwork, embedded font, generated raster, device capture, provider logo, or
private material. The `rusty-fleet` repository remains the product icon
authority; this Pages copy must be updated whenever that source changes.
