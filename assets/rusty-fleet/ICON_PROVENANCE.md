# Rusty Fleet site icon provenance

The onboarding page references:

```text
/assets/rusty-fleet/fleet-icon.svg
```

This SVG is a site-local development-preview asset created from simple vector
geometry in the Mesmer Prism Pages repository on 27 July 2026. It contains no
third-party artwork, embedded font, generated raster, device capture, or
private material.

This file is not final brand authority. The `rusty-fleet` repository owns the
canonical product and application icon. Before publication, reconcile this
site copy byte-for-byte with the canonical Fleet SVG, or replace it with a
documented deterministic derivative of that source.
